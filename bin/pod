#!/usr/bin/env macruby

if $0 == __FILE__
  require 'rubygems'
  gem 'activesupport', '~> 3.1.1'
  $:.unshift File.expand_path('../../lib', __FILE__)
end

require 'cocoapods'

Pod::Command.run(*ARGV)
