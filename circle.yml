machine:
  environment:
    LANG: en_US.UTF_8
    XCODE_PROJECT: yolo.xcodeproj
    XCODE_SCHEME: shrugs
  xcode:
    version: 6.3.1
checkout:
  post:
    - git submodule update --init
dependencies:
  override:
    - bundle install
test:
  override:
    - rake
